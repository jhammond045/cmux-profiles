name: release
on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Zip the Spoon
        run: zip -r CmuxProfiles.spoon.zip CmuxProfiles.spoon
      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: CmuxProfiles.spoon.zip
          generate_release_notes: true
